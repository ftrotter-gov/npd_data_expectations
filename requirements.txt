# FHIRTableSaw Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
sqlalchemy>=2.0.0
psycopg>=3.0.0
duckdb>=0.9.0
httpx>=0.24.0
fhir.resources>=7.0.0

# Optional but recommended
pandas>=2.0.0
great-expectations>=0.18.0
fhir4ds>=0.1.0

# Neo4j analysis (for analysis_neo4j subproject)
neo4j>=5.14.0

# Note: If fhir4ds is not available in PyPI, install from source:
# pip install git+https://github.com/CODEX-CELIDA/fhir4ds.git
